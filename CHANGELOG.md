# Changelog

All notable changes to the Phronesis Index project.

## [1.0.0] — 2026-02-08

### Added
- Core `phronesis` Python package with `PhronesisIndex`, `build_connection_laplacian`, `select_epsilon`, `adaptive_epsilon`.
- Four experiment modules: Logic Maze, Safety Gym, Multi-Robot, Scalability.
- YAML configuration files for each experiment.
- Reproduction scripts with `--smoke` mode for quick validation.
- Full LaTeX manuscript and Supplementary Information (paper-as-code with Makefile).
- Unit tests for core library.
- Docker and docker-compose support.
- GitHub Actions CI (tests + smoke tests).
- `REPRODUCIBILITY.md` with exact commands, parameters, and seed lists.
- NMI submission package (`NMI_SUBMISSION_PACKAGE/`).
- Release tag `v1.0-nmi-submission`.
